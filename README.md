# Fast-Planner-2D-ROS2
本项目实现了 FastPlanner 轨迹优化模块的独立 C++ 封装，并针对地面机器人进行了 2D 适配。 核心特性：独立非线性优化求解器调用（如 NLopt），去除了对全局地图框架的强耦合。 维度调整：将 B 样条控制点从  R 3 R  3    降维至  R 2 R  2   ，优化了针对地面运动的动力学约束算法。 可视化：提供了原生的 ROS2 接口，支持在 RViz2 中动态渲染规划轨迹与优化过程，适用于低功耗嵌入式平台的实时运动规划。
